# Client Configuration
# The base URL where the client app is running (used for OAuth callback URLs)
VITE_CLIENT_BASE_URL=http://localhost:5173

# Server Configuration
# The base URL where the server/API is running (used for auth client requests)
VITE_SERVER_BASE_URL=http://localhost:3000